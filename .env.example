# Hostname to serve, e.g. mesh.example.com. Point its DNS A record at the instance's Elastic IP
# and Caddy fetches an HTTPS certificate automatically. Keep ":80" to test over plain HTTP by IP.
DOMAIN=:80

# Largest scan upload accepted, in MB (enforced by Caddy and by /api/upload)
MAX_UPLOAD_MB=500
